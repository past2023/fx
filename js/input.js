/** Maps keyboard input to stable gameplay actions. */
export default class Input{constructor(target=window){this.keys=new Set();this.pressed=new Set();this.target=target;this.down=e=>{this.keys.add(e.code);this.pressed.add(e.code);if(['Space','ArrowUp','ArrowDown','ArrowLeft','ArrowRight'].includes(e.code))e.preventDefault()};this.up=e=>this.keys.delete(e.code);target.addEventListener('keydown',this.down);target.addEventListener('keyup',this.up)}isDown(a){return [...(a==='moveUp'?['ArrowUp','KeyW']:a==='moveDown'?['ArrowDown','KeyS']:a==='moveLeft'?['ArrowLeft','KeyA']:a==='moveRight'?['ArrowRight','KeyD']:a==='shoot'?['Space']:['Enter','Space'])].some(k=>this.keys.has(k))}consume(a){const hit=[...(a==='start'?['Enter','Space']:[a])].some(k=>this.pressed.has(k));if(hit)this.pressed.clear();return hit}endFrame(){this.pressed.clear()}destroy(){this.target.removeEventListener('keydown',this.down);this.target.removeEventListener('keyup',this.up)}}