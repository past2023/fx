/** Future-proof asset and audio abstraction. */
export async function loadAssets(){return {images:{},sounds:{}}}
export class AudioManager{playSound(name){if(name){} } playMusic(name){if(name){}} stopMusic(){}}
export default loadAssets;