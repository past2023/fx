/** Procedural two-layer recycled parallax starfield. */
export default class Background{constructor(w,h){this.w=w;this.h=h;this.layers=[this.make(90,30),this.make(55,80)]}make(n,s){return Array.from({length:n},()=>({x:Math.random()*this.w,y:Math.random()*this.h,r:Math.random()*1.8+.3,a:Math.random()*.7+.25,s}))}update(dt){for(const layer of this.layers)for(const star of layer){star.x-=star.s*dt;if(star.x<-4){star.x=this.w+Math.random()*30;star.y=Math.random()*this.h}}}render(ctx){ctx.fillStyle='#0a0a1a';ctx.fillRect(0,0,this.w,this.h);for(const layer of this.layers){ctx.fillStyle=layer===this.layers[0]?'#7085c055':'#b8f7ff99';for(const s of layer){ctx.globalAlpha=s.a;ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}}ctx.globalAlpha=1}}